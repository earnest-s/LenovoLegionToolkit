<UserControl
    x:Class="LenovoLegionToolkit.WPF.Controls.Dashboard.Modern.SystemStatusCard"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:behaviors="clr-namespace:LenovoLegionToolkit.WPF.Behaviors"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:resources="clr-namespace:LenovoLegionToolkit.WPF.Resources"
    xmlns:wpfui="http://schemas.lepo.co/wpfui/2022/xaml"
    d:DesignHeight="200"
    d:DesignWidth="400"
    mc:Ignorable="d">

    <wpfui:Card Padding="20">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="16" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <!-- Header with Icon and Title -->
            <Grid Grid.Row="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="12" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <Border
                    Grid.Column="0"
                    Width="40"
                    Height="40"
                    Background="{DynamicResource ControlFillColorDefaultBrush}"
                    CornerRadius="8">
                    <wpfui:SymbolIcon
                        x:Name="_icon"
                        FontSize="20"
                        Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                        Symbol="Desktop24" />
                </Border>

                <StackPanel Grid.Column="2" VerticalAlignment="Center">
                    <TextBlock
                        x:Name="_title"
                        FontSize="16"
                        FontWeight="SemiBold"
                        Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                        Text="Component" />
                    <TextBlock
                        x:Name="_subtitle"
                        FontSize="12"
                        Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                        Text="Status" />
                </StackPanel>

                <TextBlock
                    x:Name="_primaryValue"
                    Grid.Column="3"
                    VerticalAlignment="Center"
                    FontSize="28"
                    FontWeight="Bold"
                    Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                    Text="--" />
            </Grid>

            <!-- Metrics Grid -->
            <Grid x:Name="_metricsGrid" Grid.Row="2">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="8" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <!-- Metric 1: Utilization -->
                <StackPanel Grid.Row="0" Grid.Column="0" Margin="0,0,8,0">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <TextBlock
                            FontSize="12"
                            Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                            Text="{x:Static resources:Resource.SensorsControl_Utilization_Title}" />
                        <TextBlock
                            x:Name="_utilizationValue"
                            Grid.Column="1"
                            FontSize="12"
                            Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                            Text="-" />
                    </Grid>
                    <ProgressBar
                        x:Name="_utilizationBar"
                        Height="4"
                        Margin="0,4,0,0"
                        Maximum="100"
                        Minimum="0"
                        Value="0">
                        <i:Interaction.Behaviors>
                            <behaviors:ProgressBarAnimateBehavior />
                        </i:Interaction.Behaviors>
                    </ProgressBar>
                </StackPanel>

                <!-- Metric 2: Clock -->
                <StackPanel Grid.Row="0" Grid.Column="1" Margin="8,0,0,0">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <TextBlock
                            FontSize="12"
                            Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                            Text="{x:Static resources:Resource.SensorsControl_CoreClock_Title}" />
                        <TextBlock
                            x:Name="_clockValue"
                            Grid.Column="1"
                            FontSize="12"
                            Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                            Text="-" />
                    </Grid>
                    <ProgressBar
                        x:Name="_clockBar"
                        Height="4"
                        Margin="0,4,0,0"
                        Maximum="100"
                        Minimum="0"
                        Value="0">
                        <i:Interaction.Behaviors>
                            <behaviors:ProgressBarAnimateBehavior />
                        </i:Interaction.Behaviors>
                    </ProgressBar>
                </StackPanel>

                <!-- Metric 3: Temperature -->
                <StackPanel Grid.Row="2" Grid.Column="0" Margin="0,0,8,0">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <TextBlock
                            FontSize="12"
                            Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                            Text="{x:Static resources:Resource.SensorsControl_Temperature_Title}" />
                        <TextBlock
                            x:Name="_temperatureValue"
                            Grid.Column="1"
                            FontSize="12"
                            Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                            Text="-" />
                    </Grid>
                    <ProgressBar
                        x:Name="_temperatureBar"
                        Height="4"
                        Margin="0,4,0,0"
                        Maximum="100"
                        Minimum="0"
                        Value="0">
                        <i:Interaction.Behaviors>
                            <behaviors:ProgressBarAnimateBehavior />
                        </i:Interaction.Behaviors>
                    </ProgressBar>
                </StackPanel>

                <!-- Metric 4: Fan -->
                <StackPanel Grid.Row="2" Grid.Column="1" Margin="8,0,0,0">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <TextBlock
                            FontSize="12"
                            Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                            Text="{x:Static resources:Resource.SensorsControl_Fan_Title}" />
                        <TextBlock
                            x:Name="_fanValue"
                            Grid.Column="1"
                            FontSize="12"
                            Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                            Text="-" />
                    </Grid>
                    <ProgressBar
                        x:Name="_fanBar"
                        Height="4"
                        Margin="0,4,0,0"
                        Maximum="100"
                        Minimum="0"
                        Value="0">
                        <i:Interaction.Behaviors>
                            <behaviors:ProgressBarAnimateBehavior />
                        </i:Interaction.Behaviors>
                    </ProgressBar>
                </StackPanel>
            </Grid>
        </Grid>
    </wpfui:Card>
</UserControl>
